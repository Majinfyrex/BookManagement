{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <ul>
            <li>
                <a class="navbar-brand" href="{{ path('app_book_index') }}">Book</a>
                <a class="navbar-brand" href="{{ path('app_author_index') }}">Author</a>
                <a class="navbar-brand" href="{{ path('app_borrow_index') }}">Borrow</a>
                {% if is_granted("ROLE_ADMIN") %}
                <a class="navbar-brand" href="{{ path('app_user_index') }}">User</a>
                {% endif %}
                <a class="navbar-brand" href="{{ path('app_logout') }}">Logout</a>
            </li>
        </ul>
    </nav>
{% endblock %}